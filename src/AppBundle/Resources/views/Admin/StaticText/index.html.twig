{% extends 'AppBundle:Admin:layout.html.twig' %}

{% set page_title = 'admin.staticText.title'|trans %}

{% block main_heading %}
    <div class="page-heading">
        <h1>{{ page_title }}</h1>
        <div class="page-heading-actions">
            <a class="btn btn-add btn-lg" href="{{ url('admin_static_text_new') }}" title="{{ 'admin.staticText.actions.create'|trans }}">
                <i class="icon icon-plus-circle"></i>
            </a>
        </div>
    </div>
{% endblock %}

{% block content %}

    {{ include('AppBundle:Admin/StaticText:includes/grid.html.twig') }}

{% endblock %}
