{% trans_default_domain 'FOSUserBundle' %}

<div class="row">
    <div class="col-md-4 col-md-offset-4 well">
        <form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_start(form) }} method="POST" class="fos_user_resetting_reset">
            <div class="form-group">
                {{ form_widget(form) }}
            </div>
            <div>
                <input type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
            </div>
        </form>
    </div>
</div>